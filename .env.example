# ============================================================
# NLF POS — Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to git — only .env.example is committed
# ============================================================

# --- Required for Cloud Deployment ---
DATABASE_URL=postgresql://user:password@host:5432/nlf_pos
SECRET_KEY=your-random-secret-key-min-32-chars

# --- Cloud Platform Detection ---
# Set one of these to "true" when deploying to cloud
PRODUCTION=true
# RAILWAY_ENVIRONMENT=production    (auto-set by Railway)
# RENDER=true                       (auto-set by Render)

# --- Optional ---
PORT=8000

# ============================================================
# Store-specific settings (configured via Settings page in app)
# These are stored in the database, NOT in env vars:
#   - Store Name, Address, Phone, Email
#   - UPI ID, UPI Display Name
#   - WhatsApp Number
#   - Tax Rate, Currency Symbol
# ============================================================
